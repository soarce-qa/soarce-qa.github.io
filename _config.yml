# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.
title: SO/!\RCE
email: info@soarce-qa.de
description: >- # this means to ignore newlines until "baseurl:"
  a better overview for your PHP microservices' internals and interactions
baseurl: "" # the subpath of your site, e.g. /blog
url: "https://www.soarce-qa.de" # the base hostname & protocol for your site, e.g. http://example.com
github_username:  hsegnitz

# Build settings
markdown: kramdown
theme: jekyll-theme-slate

plugins:
#  - jekyll-feed

# Exclude from processing.
# The following items will not be processed, by default. Create a custom list
# to override the default setting.
exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor/bundle/
  - vendor/cache/
  - vendor/gems/
  - vendor/ruby/
  - .idea/
